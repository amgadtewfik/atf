# Extra dependencies for development (tests, linting).
# Install after requirements.txt:
#   uv pip install -r requirements-dev.txt
-r requirements.txt
pytest>=7.0
ruff>=0.7.0

# Optional, only needed for the torch-based code paths (see
# [project.optional-dependencies].torch in pyproject.toml):
#   uv pip install torch>=2.0
